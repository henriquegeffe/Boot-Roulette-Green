{
  "entities": {
    "UserProfile": {
      "title": "User Profile",
      "description": "Stores user roles and additional information",
      "type": "object",
      "properties": {
        "role": {
          "type": "string",
          "enum": ["admin", "user"]
        },
        "username": {
          "type": "string"
        }
      },
      "required": ["role", "username"]
    }
  },
  "firestore": {
    "users/{userId}": {
      "schema": "UserProfile",
      "description": "User profiles indexed by Auth UID"
    }
  }
}
